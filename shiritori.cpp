#include <bits/stdc++.h>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);
    std::cout.tie(0);

    int n;
	std::cin >> n;
	std::string inputs[m];
	
	for (int i = 0; i < n; i++) {
		std::cin >> inputs[i];
		if (i != 0) {
			// check if last char == first char
			// check for repitition
		}
	}
}
